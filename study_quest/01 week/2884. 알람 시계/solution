검색하고 풀었음
45분~59분일 때, 44분 이하일 때, 0시일 때
3가지 조건을 생각해냇지만 코드로 옮기기 실패,,

마지막 실패 코드
h, m = map(int, input().split())
if h==0:
    h += 24
    if 45<= m <= 59:
        print(h, m-45)
    else:
        print(h-1, m+15)
        
틀린 이유
- 24 더해주면 45~59일 때 24로 출력됨
- h!=0 상황을 다루지 않음
